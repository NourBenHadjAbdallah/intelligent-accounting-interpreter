spring.application.name=bank-statement-extractor
server.port=8080
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.thymeleaf.cache=false
```
6. Save with **Ctrl+S**

---

## If `src/main/resources` Folder Doesn't Exist Either

If you can't find `src/main/resources` in your Package Explorer:

1. Right-click the project \u2192 **New \u2192 Folder**
2. In the folder name field type:
```
src/main/resources